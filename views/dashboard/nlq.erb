<h1>Step 1. Enter a
    <i>query</i>
    and parse it.</h1>

<form id="nlqform" action="/answer" method="get" entype="multipart/form-data">

    <div>
        <input name="query" id="query" type="text" required="required" <% if @query %> value="<%= @query %>" <% end %> placeholder="Enter a natural language query."/>

        <a id="button-show-queries" title="Sample queries" class="button">
            <i class="fa fa-ellipsis-h" aria-hidden="true"></i>
        </a>
        <input name="target" type="hidden"/>
        <input id="endpoint-url" type="hidden"/>
        <input id="need-proxy" type="hidden"/>
        <button id="search-button" class="button">Search</button>
        <input id="expert-checkbox" type="checkbox" <% if @expert %>checked="checked"<% end %>/>
        <label for="expert-checkbox">expert</label>
    </div>
    <div class="nlq-options">
        <% if @expert %>
          <button id="parse-it-button" class="button">Parse it!</button>
        <% end %>
        Queries timeout in<input type="number" min="1" max="180" value="<%= @read_timeout %>" id="read_timeout" name="read_timeout">seconds.
    </div>
</form>
